# Externally visible URL for the service, used for generating ingoing links.
# Include a trailing slash.
url =

# Secret key for signing JWT cookies encoded in base64.
# Generate with Python: os.urandom(32).encode('base64')
secret =

[email]

# SMTP server:
host =

# SMTP server login:
user =
password =

# Email sender identity:
sender_name =
sender_email =
